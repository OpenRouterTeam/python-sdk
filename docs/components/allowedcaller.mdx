---
title: "AllowedCaller"
---

## Example Usage

```python
from openrouter.components import AllowedCaller

# Open enum: unrecognized values are captured as UnrecognizedStr
value: AllowedCaller = "direct"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"direct"`
- `"programmatic"`
