---
title: "BatchListItemStatus"
---

## Example Usage

```python
from openrouter.components import BatchListItemStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: BatchListItemStatus = "validating"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"validating"`
- `"in_progress"`
- `"finalizing"`
- `"completed"`
- `"failed"`
- `"expired"`
- `"cancelling"`
- `"cancelled"`
