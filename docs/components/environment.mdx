---
title: "Environment"
---

## Example Usage

```python
from openrouter.components import Environment

# Open enum: unrecognized values are captured as UnrecognizedStr
value: Environment = "windows"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"windows"`
- `"mac"`
- `"linux"`
- `"ubuntu"`
- `"browser"`
