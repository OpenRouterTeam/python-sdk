---
title: "VideoGenerationRequestOptions"
---

Provider-specific options keyed by provider slug. Only options for the matched provider are forwarded; the rest are ignored. Unrecognized keys are silently dropped.


## Fields

| Field                  | Type                   | Required               | Description            |
| ---------------------- | ---------------------- | ---------------------- | ---------------------- |
| `oneai`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `ai21`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `aion_labs`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `akashml`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `alibaba`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `amazon_bedrock`       | Dict[str, *Any*]       | ➖     | N/A                    |
| `amazon_nova`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `ambient`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `anthropic`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `anyscale`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `arcee_ai`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `atlas_cloud`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `atoma`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `avian`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `azure`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `baidu`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `baseten`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `black_forest_labs`    | Dict[str, *Any*]       | ➖     | N/A                    |
| `byteplus`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `centml`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `cerebras`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `chutes`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `cirrascale`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `clarifai`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `claude_on_aws`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `cloudflare`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `cohere`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `coreweave`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `crofai`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `crucible`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `crusoe`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `darkbloom`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `decart`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `deepgram`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `deepinfra`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `deepseek`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `dekallm`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `digitalocean`         | Dict[str, *Any*]       | ➖     | N/A                    |
| `enfer`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `fake_provider`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `featherless`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `fireworks`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `fish_audio`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `friendli`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `gmicloud`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `google_ai_studio`     | Dict[str, *Any*]       | ➖     | N/A                    |
| `google_vertex`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `gopomelo`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `groq`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `heygen`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `huggingface`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `hyperbolic`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `hyperbolic_quantized` | Dict[str, *Any*]       | ➖     | N/A                    |
| `inception`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `inceptron`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `inferact_vllm`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `inference_net`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `infermatic`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `inflection`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `inocloud`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `io_net`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `ionstream`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `klusterai`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `krea`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `lambda_`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `lepton`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `liquid`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `lynn`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `lynn_private`         | Dict[str, *Any*]       | ➖     | N/A                    |
| `mancer`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `mancer_old`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `mara`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `meta`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `minimax`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `mistral`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `modal`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `modelrun`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `modular`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `moonshotai`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `morph`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `ncompass`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `nebius`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `nex_agi`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `nextbit`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `nineteen`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `novita`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `nvidia`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `octoai`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `open_inference`       | Dict[str, *Any*]       | ➖     | N/A                    |
| `openai`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `parasail`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `perceptron`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `perplexity`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `phala`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `poolside`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `quiver`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `recraft`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `recursal`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `reflection`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `reka`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `relace`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `replicate`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `runway`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `sail_research`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `sakana`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `sakana_ai`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `sambanova`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `sambanova_cloaked`    | Dict[str, *Any*]       | ➖     | N/A                    |
| `seed`                 | Dict[str, *Any*]       | ➖     | N/A                    |
| `sf_compute`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `siliconflow`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `sourceful`            | Dict[str, *Any*]       | ➖     | N/A                    |
| `stealth`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `stepfun`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `streamlake`           | Dict[str, *Any*]       | ➖     | N/A                    |
| `switchpoint`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `targon`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `tencent`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `tenstorrent`          | Dict[str, *Any*]       | ➖     | N/A                    |
| `thinkingmachines`     | Dict[str, *Any*]       | ➖     | N/A                    |
| `together`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `together_lite`        | Dict[str, *Any*]       | ➖     | N/A                    |
| `ubicloud`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `upstage`              | Dict[str, *Any*]       | ➖     | N/A                    |
| `venice`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `voyageai`             | Dict[str, *Any*]       | ➖     | N/A                    |
| `wafer`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `wandb`                | Dict[str, *Any*]       | ➖     | N/A                    |
| `wandb_legacy`         | Dict[str, *Any*]       | ➖     | N/A                    |
| `xai`                  | Dict[str, *Any*]       | ➖     | N/A                    |
| `xiaomi`               | Dict[str, *Any*]       | ➖     | N/A                    |
| `z_ai`                 | Dict[str, *Any*]       | ➖     | N/A                    |