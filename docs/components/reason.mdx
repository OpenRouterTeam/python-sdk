---
title: "Reason"
---

## Example Usage

```python
from openrouter.components import Reason

# Open enum: unrecognized values are captured as UnrecognizedStr
value: Reason = "max_output_tokens"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"max_output_tokens"`
- `"content_filter"`
