---
title: "ImageGenerationRequestQuality"
---

Rendering quality. Providers without a quality knob ignore this.

## Example Usage

```python
from openrouter.components import ImageGenerationRequestQuality

# Open enum: unrecognized values are captured as UnrecognizedStr
value: ImageGenerationRequestQuality = "auto"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"auto"`
- `"low"`
- `"medium"`
- `"high"`
