---
title: "FinishReason"
---

## Example Usage

```python
from openrouter.components import FinishReason

# Open enum: unrecognized values are captured as UnrecognizedStr
value: FinishReason = "stop"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"stop"`
- `"length"`
- `"tool_calls"`
- `"content_filter"`
- `"function_call"`
