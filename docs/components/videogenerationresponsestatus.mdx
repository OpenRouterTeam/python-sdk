---
title: "VideoGenerationResponseStatus"
---

## Example Usage

```python
from openrouter.components import VideoGenerationResponseStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: VideoGenerationResponseStatus = "pending"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"pending"`
- `"in_progress"`
- `"completed"`
- `"failed"`
- `"cancelled"`
- `"expired"`
