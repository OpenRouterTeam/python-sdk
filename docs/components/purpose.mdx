---
title: "Purpose"
---

## Example Usage

```python
from openrouter.components import Purpose

# Open enum: unrecognized values are captured as UnrecognizedStr
value: Purpose = "assistants"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"assistants"`
- `"batch"`
- `"fine-tune"`
- `"vision"`
- `"user_data"`
- `"evals"`
- `"assistants_output"`
- `"batch_output"`
- `"fine-tune-results"`
