---
title: "SupportedFrameImage"
---

## Example Usage

```python
from openrouter.components import SupportedFrameImage

# Open enum: unrecognized values are captured as UnrecognizedStr
value: SupportedFrameImage = "first_frame"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"first_frame"`
- `"last_frame"`
