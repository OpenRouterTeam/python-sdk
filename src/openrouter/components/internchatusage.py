"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .internchatprompttokensdetails import (
    InternChatPromptTokensDetails,
    InternChatPromptTokensDetailsTypedDict,
)
from openrouter.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class InternChatUsageTypedDict(TypedDict):
    r"""Token usage for the run as the daemon reported it, on the final chunk before `[DONE]`. `null` when the daemon reported none, and always `null` after `tool_calls` because the turn is not over."""

    completion_tokens: int
    prompt_tokens: int
    total_tokens: int
    cost: NotRequired[float]
    r"""The cost of the run in USD, when the daemon reported one."""
    prompt_tokens_details: NotRequired[InternChatPromptTokensDetailsTypedDict]
    r"""Prompt tokens served from cache, when the daemon reported them."""


class InternChatUsage(BaseModel):
    r"""Token usage for the run as the daemon reported it, on the final chunk before `[DONE]`. `null` when the daemon reported none, and always `null` after `tool_calls` because the turn is not over."""

    completion_tokens: int

    prompt_tokens: int

    total_tokens: int

    cost: Optional[float] = None
    r"""The cost of the run in USD, when the daemon reported one."""

    prompt_tokens_details: Optional[InternChatPromptTokensDetails] = None
    r"""Prompt tokens served from cache, when the daemon reported them."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["cost", "prompt_tokens_details"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
