"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .batchdeletionoutcome import BatchDeletionOutcome
from openrouter.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import NotRequired, TypedDict


Openrouter = Literal["deleted",]
r"""OpenRouter-held request and result artifacts were purged."""


class UpstreamTypedDict(TypedDict):
    r"""The upstream batch deletion outcome; omitted when no provider was assigned."""

    provider: str
    r"""The provider name stored on the batch."""
    status: BatchDeletionOutcome
    r"""Outcome for one deletion target: `deleted` (removed), `unsupported` (the provider has no batch-delete API; supported file cleanup still runs), or `not_applicable` (the batch never reached that target)."""


class Upstream(BaseModel):
    r"""The upstream batch deletion outcome; omitted when no provider was assigned."""

    provider: str
    r"""The provider name stored on the batch."""

    status: BatchDeletionOutcome
    r"""Outcome for one deletion target: `deleted` (removed), `unsupported` (the provider has no batch-delete API; supported file cleanup still runs), or `not_applicable` (the batch never reached that target)."""


class BatchDeletionTargetsTypedDict(TypedDict):
    r"""OpenRouter cleanup and, when a provider was assigned, the upstream batch deletion outcome."""

    openrouter: Openrouter
    r"""OpenRouter-held request and result artifacts were purged."""
    upstream: NotRequired[UpstreamTypedDict]
    r"""The upstream batch deletion outcome; omitted when no provider was assigned."""


class BatchDeletionTargets(BaseModel):
    r"""OpenRouter cleanup and, when a provider was assigned, the upstream batch deletion outcome."""

    openrouter: Openrouter
    r"""OpenRouter-held request and result artifacts were purged."""

    upstream: Optional[Upstream] = None
    r"""The upstream batch deletion outcome; omitted when no provider was assigned."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["upstream"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
