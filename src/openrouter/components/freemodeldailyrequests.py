"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from openrouter.types import BaseModel
from typing_extensions import TypedDict


class FreeModelDailyRequestsTypedDict(TypedDict):
    r"""Free-model (`:free` variant) daily request quota for the account that owns the key. Reports the same counter and tier limit that free-model enforcement reads for accounts subject to the free-model limits; the counter resets at UTC midnight. Accounts and endpoints exempt from free-model limits, and BYOK requests, are not gated by it, so `remaining` is the tier policy rather than an enforced ceiling for them."""

    limit: int
    r"""Free-model requests the account may make per UTC day; the ceiling depends on total credits purchased and is independent of `is_free_tier`"""
    remaining: int
    r"""Free-model requests left in the current UTC day"""
    used: int
    r"""Free-model requests recorded for the account so far in the current UTC day"""


class FreeModelDailyRequests(BaseModel):
    r"""Free-model (`:free` variant) daily request quota for the account that owns the key. Reports the same counter and tier limit that free-model enforcement reads for accounts subject to the free-model limits; the counter resets at UTC midnight. Accounts and endpoints exempt from free-model limits, and BYOK requests, are not gated by it, so `remaining` is the tier policy rather than an enforced ceiling for them."""

    limit: int
    r"""Free-model requests the account may make per UTC day; the ceiling depends on total credits purchased and is independent of `is_free_tier`"""

    remaining: int
    r"""Free-model requests left in the current UTC day"""

    used: int
    r"""Free-model requests recorded for the account so far in the current UTC day"""
