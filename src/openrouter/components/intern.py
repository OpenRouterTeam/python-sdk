"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from openrouter.types import BaseModel, Nullable, UNSET_SENTINEL, UnrecognizedStr
from pydantic import model_serializer
from typing import Literal, Union
from typing_extensions import TypedDict


class ProgressTypedDict(TypedDict):
    r"""Active provisioning step, or null once provisioning has settled."""

    step_label: str
    r"""Human-readable label of the active provisioning step."""
    step_number: int
    r"""One-based index of the active step."""
    total_steps: int
    r"""Number of provisioning steps."""


class Progress(BaseModel):
    r"""Active provisioning step, or null once provisioning has settled."""

    step_label: str
    r"""Human-readable label of the active provisioning step."""

    step_number: int
    r"""One-based index of the active step."""

    total_steps: int
    r"""Number of provisioning steps."""


InternStatus = Union[
    Literal[
        "awaiting_slack_install",
        "queued",
        "provisioning",
        "running",
        "failed",
        "stopped",
        "destroying",
        "destroy_failed",
    ],
    UnrecognizedStr,
]
r"""Lifecycle status."""


class InternTypedDict(TypedDict):
    r"""Public lifecycle state and settings for one intern."""

    attached_vault_id: Nullable[str]
    r"""Vault the intern borrows from another intern, or null when it borrows none."""
    created_at: str
    r"""ISO 8601 creation time."""
    description: Nullable[str]
    r"""Free-form description."""
    hostname: Nullable[str]
    r"""Public hostname the intern is reachable at, or null until provisioning has assigned one."""
    id: str
    r"""Intern id."""
    instructions: Nullable[str]
    r"""Standing instructions the intern boots with."""
    last_failure_message: Nullable[str]
    r"""Why the last provisioning attempt failed, when status is failed."""
    model: Nullable[str]
    r"""OpenRouter model slug the intern runs, or null for the workspace default."""
    name: str
    r"""Intern name, unique per creator within a workspace."""
    progress: Nullable[ProgressTypedDict]
    r"""Active provisioning step, or null once provisioning has settled."""
    status: InternStatus
    r"""Lifecycle status."""
    updated_at: str
    r"""ISO 8601 last update time."""
    vault_id: Nullable[str]
    r"""Vault the intern owns, or null before it has been created."""
    workspace_id: str
    r"""Workspace that owns the intern and scopes its secrets."""


class Intern(BaseModel):
    r"""Public lifecycle state and settings for one intern."""

    attached_vault_id: Nullable[str]
    r"""Vault the intern borrows from another intern, or null when it borrows none."""

    created_at: str
    r"""ISO 8601 creation time."""

    description: Nullable[str]
    r"""Free-form description."""

    hostname: Nullable[str]
    r"""Public hostname the intern is reachable at, or null until provisioning has assigned one."""

    id: str
    r"""Intern id."""

    instructions: Nullable[str]
    r"""Standing instructions the intern boots with."""

    last_failure_message: Nullable[str]
    r"""Why the last provisioning attempt failed, when status is failed."""

    model: Nullable[str]
    r"""OpenRouter model slug the intern runs, or null for the workspace default."""

    name: str
    r"""Intern name, unique per creator within a workspace."""

    progress: Nullable[Progress]
    r"""Active provisioning step, or null once provisioning has settled."""

    status: InternStatus
    r"""Lifecycle status."""

    updated_at: str
    r"""ISO 8601 last update time."""

    vault_id: Nullable[str]
    r"""Vault the intern owns, or null before it has been created."""

    workspace_id: str
    r"""Workspace that owns the intern and scopes its secrets."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m
