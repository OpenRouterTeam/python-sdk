"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .internchattoolcall import InternChatToolCall, InternChatToolCallTypedDict
from openrouter.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


InternChatDeltaRole = Literal["assistant",]


class InternChatDeltaTypedDict(TypedDict):
    r"""The incremental content of one chunk. The first chunk carries `role`, text chunks carry `content`, reasoning chunks carry `reasoning`, and an interaction chunk carries one complete `tool_calls` entry."""

    content: NotRequired[str]
    reasoning: NotRequired[str]
    role: NotRequired[InternChatDeltaRole]
    tool_calls: NotRequired[List[InternChatToolCallTypedDict]]


class InternChatDelta(BaseModel):
    r"""The incremental content of one chunk. The first chunk carries `role`, text chunks carry `content`, reasoning chunks carry `reasoning`, and an interaction chunk carries one complete `tool_calls` entry."""

    content: Optional[str] = None

    reasoning: Optional[str] = None

    role: Optional[InternChatDeltaRole] = None

    tool_calls: Optional[List[InternChatToolCall]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["content", "reasoning", "role", "tool_calls"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
