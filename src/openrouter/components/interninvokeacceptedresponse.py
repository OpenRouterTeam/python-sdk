"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from openrouter.types import BaseModel, UnrecognizedStr
from typing import Literal, Union
from typing_extensions import TypedDict


InternInvokeAcceptedResponseStatus = Union[
    Literal[
        "started",
        "steered",
    ],
    UnrecognizedStr,
]
r"""`started` when a new run began. `steered` when the session already had a run going and the prompt was delivered into it instead."""


class InternInvokeAcceptedResponseTypedDict(TypedDict):
    r"""The intern admitted the prompt. The run continues on the intern."""

    session_id: str
    r"""The session the run belongs to. Send it back to continue the conversation."""
    status: InternInvokeAcceptedResponseStatus
    r"""`started` when a new run began. `steered` when the session already had a run going and the prompt was delivered into it instead."""


class InternInvokeAcceptedResponse(BaseModel):
    r"""The intern admitted the prompt. The run continues on the intern."""

    session_id: str
    r"""The session the run belongs to. Send it back to continue the conversation."""

    status: InternInvokeAcceptedResponseStatus
    r"""`started` when a new run began. `steered` when the session already had a run going and the prompt was delivered into it instead."""
